# Development Environment Configuration
# This file is automatically loaded when running: npm run dev
# or when NODE_ENV=development

# Demo mode - disabled by default for development
VITE_DEMO_MODE=true

# Use real prover for actual testnet4 transactions
VITE_USE_MOCK_PROVER=true

# Development prover endpoint - via Vite's built-in proxy to avoid CORS
# This proxies /api/charms/* to https://v8.charms.dev/* (see vite.config.ts)
VITE_PROVER_ENDPOINT=/api/charms/spells/prove

# Logging configuration
# Set global level: DEBUG, INFO, WARN, ERROR
# Or per-module: CharmsService:DEBUG,ProverService:INFO
# Examples:
#   VITE_LOG_LEVEL=DEBUG           # All modules at DEBUG level
#   VITE_LOG_LEVEL=WARN            # All modules at WARN level (quiet)
#   VITE_LOG_LEVEL=CharmsService:DEBUG,ProverService:INFO  # Per-module
VITE_LOG_LEVEL=DEBUG
