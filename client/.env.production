# Production Environment Configuration
# This file is automatically loaded when running: npm run build
# or when NODE_ENV=production

# Demo mode - disabled for production
VITE_DEMO_MODE=false

# Use real prover (not mock)
VITE_USE_MOCK_PROVER=false

# Production prover endpoint
# NOTE: This is the fallback value for local builds
# In CI/CD (GitHub Actions), this is overridden by the VITE_PROVER_ENDPOINT secret
#
# Options:
# 1. Direct API (has CORS issues): https://v8.charms.dev/spells/prove
# 2. Deployed proxy (recommended): https://your-backend.com/spells/prove
#
# For GitHub Actions deployment, set the secret at:
# Settings > Secrets and variables > Actions > Repository secrets > New repository secret
VITE_PROVER_ENDPOINT=https://v8.charms.dev/spells/prove

# Production logging - only warnings and errors
VITE_LOG_LEVEL=DEBUG
